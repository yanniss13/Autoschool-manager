{% extends 'layouts/base.twig' %}

{% block content %}
  <section class="form-card">
    <h1>Connexion élève</h1>

    {% if errors.global %}<div class="flash flash-error">{{ errors.global }}</div>{% endif %}

    <form action="/student-login" method="post" novalidate>
      <input type="hidden" name="_csrf" value="{{ csrfToken }}">
      <div class="form-group">
        <label for="email">Email élève</label>
        <input type="email" id="email" name="email"
               autocomplete="username" value="{{ values.email|default('') }}" required>
        {% if errors.email %}<p class="field-error">{{ errors.email }}</p>{% endif %}
      </div>

      <div class="form-group">
        <label for="password">Mot de passe</label>
        <input type="password" id="password" name="password"
               autocomplete="current-password" required>
        {% if errors.password %}<p class="field-error">{{ errors.password }}</p>{% endif %}
      </div>

      <button type="submit" class="btn btn-primary">Se connecter</button>
    </form>

    <p class="form-footer"><a href="/forgot-password">Mot de passe oublié ?</a></p>
    <p class="form-footer">Espace réservé aux élèves créés par le gérant.</p>
  </section>
{% endblock %}
