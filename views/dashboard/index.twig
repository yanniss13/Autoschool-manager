{% extends 'layouts/base.twig' %}

{% block content %}
  <section class="hero">
    <h1>Tableau de bord</h1>
    <p>Bienvenue, <strong>{{ currentCompany.businessName }}</strong>.</p>

    <ul class="info-list">
      <li><span class="info-label">Raison sociale :</span> {{ currentCompany.businessName }}</li>
      <li><span class="info-label">SIRET :</span> {{ currentCompany.siret }}</li>
      {% if currentCompany.directorName %}
        <li><span class="info-label">Directeur :</span> {{ currentCompany.directorName }}</li>
      {% endif %}
    </ul>
  </section>

  <section class="stats-grid">
    <div class="stat-card">
      <span class="stat-value">{{ stats.employees }}</span>
      <span class="stat-label">Employés</span>
    </div>
    <div class="stat-card">
      <span class="stat-value">{{ stats.students }}</span>
      <span class="stat-label">Élèves</span>
    </div>
    <div class="stat-card">
      <span class="stat-value">{{ stats.vehicles }}</span>
      <span class="stat-label">Véhicules</span>
    </div>
    <div class="stat-card">
      <span class="stat-value">{{ stats.assigned }}</span>
      <span class="stat-label">Véhicules affectés</span>
    </div>
    <div class="stat-card">
      <span class="stat-value">{{ stats.available }}</span>
      <span class="stat-label">Véhicules disponibles</span>
    </div>
  </section>

  <div class="dashboard-actions">
    <a href="/employees" class="btn btn-primary">Gérer les employés</a>
    <a href="/vehicles" class="btn btn-primary">Gérer les véhicules</a>
    <a href="/students" class="btn btn-primary">Gérer les élèves</a>
  </div>
{% endblock %}
